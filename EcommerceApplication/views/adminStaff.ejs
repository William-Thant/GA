<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Manage Staff - Ecommerce</title>
    <style>
        body { background-color: #f8f9fa; }
        .staff-container { max-width: 900px; margin: 2rem auto; background: white; padding: 2rem; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.08); }
    </style>
    <link rel='stylesheet' href='/style.css'>
</head>
<body class="page-shell">
    <%- include('partials/topbar') %>
    <%- include('partials/navbar') %>
    <div class="container">
        <div class="staff-container" data-staff-admin data-escrow-address="<%= contractAddress || '' %>">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="mb-0">Manage Staff</h1>
                <a class="btn btn-outline-secondary" href="/admin/orders">Back to Orders</a>
            </div>

            <div class="alert alert-secondary py-2 mb-3">
                <div><strong>Wallet:</strong> <span data-wallet-status>Not connected</span></div>
                <div><strong>Address:</strong> <span data-wallet-address>-</span></div>
                <div class="small text-muted">Must be the contract owner to add/remove staff.</div>
                <button type="button" class="btn btn-sm btn-outline-dark mt-2" data-connect-wallet>
                    Connect MetaMask
                </button>
                <div class="small text-danger mt-2" data-staff-status></div>
            </div>

            <form class="row g-3" data-staff-form>
                <div class="col-md-8">
                    <label for="staffAddress" class="form-label">Staff Wallet Address</label>
                    <input type="text" class="form-control" id="staffAddress" required placeholder="0x...">
                </div>
                <div class="col-md-4">
                    <label for="staffAction" class="form-label">Action</label>
                    <select class="form-select" id="staffAction" required>
                        <option value="true">Allow</option>
                        <option value="false">Remove</option>
                    </select>
                </div>
                <div class="col-12">
                    <button type="submit" class="btn btn-primary">Update Staff</button>
                </div>
            </form>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/ethers@6.10.0/dist/ethers.umd.min.js"></script>
    <script src="/js/admin-staff.js"></script>
</body>
</html>



