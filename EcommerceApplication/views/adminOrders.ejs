<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Admin Orders - Ecommerce</title>
    <style>
        body { background-color: #f8f9fa; }
        .admin-container { max-width: 1000px; margin: 2rem auto; background: white; padding: 2rem; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.08); }
        .order-card { border: 1px solid #e3e3e3; border-radius: 8px; padding: 1rem; margin-bottom: 1rem; }
        .badge-status { font-size: 0.85rem; }
    </style>
    <link rel='stylesheet' href='/style.css'>
</head>
<body class="page-shell">
    <%- include('partials/topbar') %>
    <%- include('partials/navbar') %>
    <div class="container">
        <div class="admin-container">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="mb-0">Admin Orders</h1>
                <a class="btn btn-outline-secondary" href="/admin/staff">Manage Staff</a>
            </div>

            <div class="alert alert-secondary py-2 mb-4">
                <div class="small text-muted">Admin controls delivery status. Buyer can confirm only after you mark an order as OUT_FOR_DELIVERY.</div>
            </div>

            <% if (!orders || orders.length === 0) { %>
                <div class="alert alert-info">No orders yet.</div>
            <% } else { %>
                <% orders.forEach(order => { %>
                    <div class="order-card">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <div><strong>Order ID:</strong> <%= order.orderId %></div>
                                <div><strong>Status:</strong>
                                    <span class="badge bg-secondary badge-status"><%= order.status %></span>
                                </div>
                                <div><strong>Delivery Status:</strong>
                                    <span class="badge bg-info badge-status"><%= order.deliveryStatus || "PENDING" %></span>
                                </div>
                                <div><strong>Buyer Wallet:</strong> <%= order.buyerWallet || "-" %></div>
                                <div><strong>Tx Hash:</strong> <%= order.txHash || "-" %></div>
                                <div><strong>Created:</strong> <%= order.createdAt || "-" %></div>
                            </div>
                            <form action="/admin/orders/<%= order.orderId %>/status" method="POST" class="d-flex gap-2">
                                <select class="form-select" name="status" required>
                                    <option value="PAID_ESCROW" <%= order.status === "PAID_ESCROW" ? "selected" : "" %>>PAID_ESCROW</option>
                                    <option value="SHIPPED" <%= order.status === "SHIPPED" ? "selected" : "" %>>SHIPPED</option>
                                    <option value="DELIVERED" <%= order.status === "DELIVERED" ? "selected" : "" %>>DELIVERED</option>
                                    <option value="RELEASED" <%= order.status === "RELEASED" ? "selected" : "" %>>RELEASED</option>
                                    <option value="REFUNDED" <%= order.status === "REFUNDED" ? "selected" : "" %>>REFUNDED</option>
                                </select>
                                <button type="submit" class="btn btn-primary">Update</button>
                            </form>
                        </div>

                        <div class="mt-3">
                            <form action="/admin/orders/<%= order.orderId %>/delivery" method="POST" class="d-flex gap-2">
                                <select class="form-select" name="status" required>
                                    <option value="PENDING" <%= (order.deliveryStatus || "PENDING") === "PENDING" ? "selected" : "" %>>PENDING</option>
                                    <option value="OUT_FOR_DELIVERY" <%= order.deliveryStatus === "OUT_FOR_DELIVERY" ? "selected" : "" %>>OUT_FOR_DELIVERY</option>
                                    <option value="DELIVERED" <%= order.deliveryStatus === "DELIVERED" ? "selected" : "" %>>DELIVERED</option>
                                </select>
                                <button type="submit" class="btn btn-outline-primary">Update Delivery</button>
                            </form>
                        </div>

                        <% if (order.items && order.items.length) { %>
                            <hr>
                            <div><strong>Items:</strong></div>
                            <ul class="mb-0">
                                <% order.items.forEach(item => { %>
                                    <li><%= item.name || item.productId %> (x<%= item.quantity || 1 %>)</li>
                                <% }); %>
                            </ul>
                        <% } %>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>



